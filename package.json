{
  "name": "nirvik-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build && rm -rf dist && cp -R frontend/dist dist"
  }
}
